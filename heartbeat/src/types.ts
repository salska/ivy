/**
 * ivy-heartbeat's own types.
 *
 * Domain types (Agent, Project, WorkItem, Event, Heartbeat) are provided
 * by ivy-blackboard and re-exported from blackboard.ts.
 * This file only contains ivy-heartbeat-specific types.
 */

export type { ListOptions } from './repositories/events.ts';
