name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Check out ivy-blackboard (sibling dependency)
        run: git clone --depth 1 https://github.com/jcfischer/ivy-blackboard.git ../ivy-blackboard

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install ivy-blackboard dependencies
        run: bun install --cwd ../ivy-blackboard

      - name: Install dependencies
        run: bun install

      - name: Run tests
        run: bun test
