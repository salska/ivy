{
  "name": "ivy-blackboard",
  "version": "0.1.0",
  "description": "Local agent blackboard — SQLite-based multi-agent coordination using the blackboard pattern",
  "type": "module",
  "main": "src/index.ts",
  "bin": {
    "blackboard": "src/index.ts"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "test": "bun test",
    "build": "bun build src/index.ts --outdir dist --target node"
  },
  "dependencies": {
    "commander": "^14.0.3",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7"
  },
  "license": "MIT"
}
