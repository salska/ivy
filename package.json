{
  "name": "ivy-root",
  "workspaces": [
    "blackboard",
    "heartbeat"
  ],
  "scripts": {
    "build": "cd heartbeat && bun run build && cd ../blackboard && bun run build",
    "serve": "cd blackboard && bun run dev serve --dispatch"
  }
}